%span{:class => "activity activity-#{activity.trackable_type.underscore.dasherize} badge badge-light"}
  Updated #{time_ago_in_words(activity.created_at)} ago. #{"(#{activity.owner.full_name})" if activity.owner}
