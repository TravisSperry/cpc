<div class="container">
  <div class="row">
    <div class="col-sm-12">
      <h1>
        <% if @customer %>
          <%= "New #{@customer.name} Contact" %>
        <% else %>
          New Contact
        <% end %>
      </h1>
    </div>
  </div>

  <%= render 'form' %>
</div>
